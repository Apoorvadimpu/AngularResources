import { UseunlessDirective } from './useunless.directive';

describe('UseunlessDirective', () => {
  it('should create an instance', () => {
    const directive = new UseunlessDirective();
    expect(directive).toBeTruthy();
  });
});
